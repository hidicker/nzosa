{
  "name": "@nzosa/core",
  "version": "0.1.0",
  "description": "Zero-dependency core: bank flat-file parsers, canonical transaction schema, and deduplication.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "npm run build && node --test \"test/*.test.js\""
  }
}
